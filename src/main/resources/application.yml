batch:
  datasource:
    url: jdbc:h2:file:./data/batchdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jdbc:
    initialize.schema: always

spring:

  batch:
    jdbc:
      initialize-schema: always

  h2:
    console:
      enabled: true
      path: /h2-console

app:

  datasource-a:
    url: jdbc:h2:file:./data/dba/dbA
    driver-class-name: org.h2.Driver
    username: sa
    password:

  datasource-b:
    url: jdbc:h2:file:./data/dbb/dbB
    driver-class-name: org.h2.Driver
    username: sa
    password: